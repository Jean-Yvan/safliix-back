# mappers

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build mappers` to build the library.

## Running unit tests

Run `nx test mappers` to execute the unit tests via [Jest](https://jestjs.io).
